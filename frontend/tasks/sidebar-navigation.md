#/src/widgets/SidebarNavigation/SidebarNavigation.tsx привести к соответствию с дизайном из Figma https://www.figma.com/design/pUzKpwp5I99DEA86eA6qR7/Pulse-Platform-AI--Copy-?node-id=840-14734&m=dev
Должны быт следующие элементы:

- Game (Icon name `game`)
- Forecast (Icon name `forecast`)
- Result (Icon name `result`)
- Wallet c (Icon name `wallet`) + label `coming soon`, роут не нужен
- VPN (Icon name `vpn`) + label `coming soon`, роут не нужен
- Charity (Icon name `charity`) + label `coming soon`, роут не нужен
- Bank Card (Icon name `bank-card`) + label `coming soon`, роут не нужен
- Tasks (Icon name `tasks`)
- PreSale (Icon name `presale`) - создать соответствующий роут в `/src/routes`
- Profile (Icon name `profile`) - создать соответствующий роут в `/src/routes`
- Rules (Icon name `rules`) - создать соответствующий роут в `/src/routes`
- Help (Icon name `help`) - создать соответствующий роут в `/src/routes`

Цвет фона, размер шрифтов должен быть такими же как в дизайне.
При клике на More из #src/wigets/BottomNavigation/BottomNavigation.tsx должен открыться SidebarNavigation, при повторном клике должен закрываться.
