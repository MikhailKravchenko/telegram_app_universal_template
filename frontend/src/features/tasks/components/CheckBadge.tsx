import {Icon} from '@/components/Icon';

export const CheckBadge = () => (
    <div className="w-10 h-10 rounded-[12px] bg-[var(--accent)] grid place-items-center">
        <Icon name="check" size={24} />
    </div>
);
